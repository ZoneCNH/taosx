{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "taosx Error",
  "type": "object",
  "required": ["kind", "op", "message", "retryable"],
  "properties": {
    "kind": {
      "enum": [
        "config",
        "validation",
        "connection",
        "unavailable",
        "timeout",
        "auth",
        "conflict",
        "rate_limit",
        "sql",
        "schema",
        "write",
        "internal"
      ]
    },
    "op": {
      "type": "string"
    },
    "message": {
      "type": "string"
    },
    "retryable": {
      "type": "boolean"
    }
  }
}
